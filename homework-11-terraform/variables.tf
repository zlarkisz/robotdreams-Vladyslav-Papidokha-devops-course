# =============================================================================
# VARIABLES.TF — Глобальні змінні проекту
# =============================================================================
#
# Ці змінні використовуються в main.tf і передаються в модулі.
# Можна задати значення:
#   1. За замовчуванням (default) — тут
#   2. Через файл terraform.tfvars
#   3. Через командний рядок: terraform apply -var="aws_region=us-east-1"
#   4. Через змінну середовища: TF_VAR_aws_region=us-east-1
# =============================================================================


# -----------------------------------------------------------------------------
# AWS Region
# -----------------------------------------------------------------------------
# В якому регіоні створювати ресурси.
# eu-central-1 = Frankfurt (найближчий до України)

variable "aws_region" {
  description = "AWS регіон для створення ресурсів"
  type        = string
  default     = "eu-central-1"
}


# -----------------------------------------------------------------------------
# Project Name
# -----------------------------------------------------------------------------
# Назва проекту — використовується для назв ресурсів і тегів.
# Допомагає знайти свої ресурси в AWS Console.

variable "project_name" {
  description = "Назва проекту для тегування"
  type        = string
  default     = "terraform-hw"
}


# -----------------------------------------------------------------------------
# VPC CIDR
# -----------------------------------------------------------------------------
# Діапазон IP-адрес для VPC.

variable "vpc_cidr" {
  description = "CIDR блок для VPC"
  type        = string
  default     = "10.0.0.0/16"
}


# -----------------------------------------------------------------------------
# Subnet CIDRs
# -----------------------------------------------------------------------------
# Діапазони IP-адрес для підмереж.
# Повинні бути в межах VPC CIDR!

variable "public_subnet_cidr" {
  description = "CIDR блок для публічної підмережі"
  type        = string
  default     = "10.0.1.0/24"
}

variable "private_subnet_cidr" {
  description = "CIDR блок для приватної підмережі"
  type        = string
  default     = "10.0.2.0/24"
}


# -----------------------------------------------------------------------------
# Availability Zone
# -----------------------------------------------------------------------------
# В якому дата-центрі створювати ресурси.

variable "availability_zone" {
  description = "Availability Zone"
  type        = string
  default     = "eu-central-1a"
}


# -----------------------------------------------------------------------------
# EC2 Instance Type
# -----------------------------------------------------------------------------
# Тип серверів. t2.micro = Free Tier!

variable "instance_type" {
  description = "Тип EC2 інстансу"
  type        = string
  default     = "t2.micro"
}